{
  "name": "backend-oefeningen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "camelcase index.js",
    "precompress": "{{ executes BEFORE the `compress` script }}",
    "compress": "{{ run command to compress files }}",
    "postcompress": "{{ executes AFTER `compress` script }}"
  },
  "author": "Michael Wilhelm",
  "license": "MIT",
  "dependencies": {
    "camelcase": "^6.3.0",
    "ejs": "^3.1.6",
    "express": "^4.17.2",
    "server": "^0.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
